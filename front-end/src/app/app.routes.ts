import { Routes } from '@angular/router';
import { SolicReportComponent } from './components/solic-report/solic-report.component';

export const routes: Routes = [
    {
        path: 'solic-report',
        component: SolicReportComponent,
    },
];
